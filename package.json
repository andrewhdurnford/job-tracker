{
  "name": "job-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Polls company ATS boards for new job postings, publishes a static site, emails on new roles.",
  "scripts": {
    "poll": "node src/poll.js",
    "test": "node --test src/*.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "engines": {
    "node": ">=20"
  }
}
